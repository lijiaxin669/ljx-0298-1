version: '3.8'

services:
  lantern-gallery:
    build: .
    container_name: lantern-gallery
    ports:
      - "8080:80"
    restart: unless-stopped
    mem_limit: 512m
    mem_reservation: 256m
    cpus: '0.5'
